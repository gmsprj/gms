#!/bin/bash
#
# DBの全てのテーブル定義を出力する。
#
DBUSER="$GM_DBUSER"
DBNAME="$GM_DBNAME"
DBPASS="$GM_DBPASS"

echo db-user: $DBUSER
echo db-name: $DBNAME

mysqldump -u $DBUSER --no-data $DBNAME -p$DBPASS > $DBNAME.all-table-structures.sql

