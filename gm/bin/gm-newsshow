#!/bin/bash
#
# DBのテーブルレコードを表示する。
#
DBUSER="$GM_DBUSER"
DBNAME="$GM_DBNAME"
TABNAME="$1"

mysql -u $DBUSER -p $DBNAME <<EOF
SELECT content FROM cells A INNER JOIN texts B ON A.left_id = B.id WHERE A.name = 'news';
EOF
