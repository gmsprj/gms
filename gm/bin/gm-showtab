#!/bin/bash
#
# DBのテーブルレコードを表示する。
#
DBUSER="$GM_DBUSER"
DBNAME="$GM_DBNAME"
DBPASS="$GM_DBPASS"
TABNAME="$1"

echo db-user: $DBUSER
echo db-name: $DBNAME

mysql -u $DBUSER -p$DBPASS $DBNAME <<EOF
SELECT * FROM $TABNAME;
EOF

